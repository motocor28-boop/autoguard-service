plugins {
    id 'com.android.application'
}

android {
    namespace 'cl.nexosecure.demo'
    compileSdk 35

    defaultConfig {
        applicationId 'cl.nexosecure.demo'
        minSdk 26
        targetSdk 35
        versionCode 2
        versionName '0.2-test'
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
