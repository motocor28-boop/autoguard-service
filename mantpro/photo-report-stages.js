/* MANTPRO IA Cloud — fotografías opcionales con designación en todos los informes. */
(()=>{
  'use strict';
  const BUILD='2026-07-23-optional-stage-photos';
  const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
  const fmt=v=>{if(!v)return'No registrado';const d=new Date(v);return Number.isNaN(d.valueOf())?'No registrado':d.toLocaleString('es-CL',{dateStyle:'short',timeStyle:'short',hour12:false})};
  const records=()=>window.MANTPRO?.records?.()||[];
  const byType=t=>records().filter(x=>x.type===t);
  const linked=(t,id)=>byType(t).filter(x=>x.data?.jobId===id);
  const supervisor=()=>window.MANTPRO_CONFIG?.supervisor||'Esteban Cortez Richards';
  const dateKey=v=>{const d=new Date(v||Date.now());return Number.isNaN(d.valueOf())?'':[d.getFullYear(),String(d.getMonth()+1).padStart(2,'0'),String(d.getDate()).padStart(2,'0')].join('-')};
  const recordDate=x=>{const d=x?.data||{};return d.recordedAt||d.actualStart||d.startAt||d.plannedStart||x?.at||''};
  const selectedDay=()=>$('#ehs-report-date')?.value||$('#report-form [name="date"]')?.value||sessionStorage.getItem('mantpro-report-day')||new Date().toISOString().slice(0,10);
  const sameDay=(x,day)=>dateKey(recordDate(x))===day;
  const uniqueEntries=entries=>{const seen=new Set();return entries.filter(x=>{const key=x.src;if(!key||seen.has(key))return false;seen.add(key);return true})};

  function rawPhoto(value){if(!value)return'';if(typeof value==='string')return value;if(Array.isArray(value))return rawPhoto(value[0]);if(typeof value==='object')return rawPhoto(value.dataUrl||value.dataURL||value.src||value.url||value.photo||value.image||value.base64||value.content||'');return''}
  function legacyPhotos(record){if(!record)return[];const d=record.data||{},values=[record.photo,record.image,record.photos,record.images,record.attachments,d.photo,d.image,d.photos,d.images,d.attachments,d.photoData,d.imageData,d.evidence,d.evidences],out=[];const add=v=>{if(!v)return;if(Array.isArray(v)){v.forEach(add);return}if(typeof v==='object'&&!rawPhoto(v)){Object.values(v).forEach(add);return}const p=rawPhoto(v);if(p&&!out.includes(p))out.push(p)};values.forEach(add);return out}
  function stagePhotos(record){return Array.isArray(record?.data?.stagePhotos)?record.data.stagePhotos.filter(x=>rawPhoto(x)):[]}
  function photoCandidates(record){return uniqueEntries([...stagePhotos(record).map(x=>({src:rawPhoto(x)})),...legacyPhotos(record).map(src=>({src}))]).map(x=>x.src)}
  function entriesFrom(record,label,kind,context=''){const entries=[];stagePhotos(record).forEach((p,i)=>entries.push({record,src:rawPhoto(p),label:p.designation||`${label} · Foto ${i+1}`,kind:p.segment||kind,context:p.note||context,capturedAt:p.capturedAt||record.at}));legacyPhotos(record).forEach((src,i)=>entries.push({record,src,label:i?`${label} · Foto ${i+1}`:label,kind,context,capturedAt:record.at}));return uniqueEntries(entries)}
  function photoEntriesForSafety(safety){const d=safety?.data||{},label=d.photoType||`${d.category||'Desviación'} · ${d.area||'Área no informada'}`;return entriesFrom(safety,label,'Seguridad',d.text||'')}
  function photoEntriesForWalk(walk){if(!walk)return[];const d=walk.data||{},entries=[...entriesFrom(walk,`Caminata · ${d.area||'Área no informada'}`,'Caminata',d.objective||'')];byType('safety').filter(x=>x.data?.walkId===walk.id).sort((a,b)=>new Date(a.at)-new Date(b.at)).forEach((x,i)=>photoEntriesForSafety(x).forEach(p=>entries.push({...p,label:p.label.startsWith('Caminata')?p.label:`Caminata · Hallazgo ${i+1} · ${p.label}`})));return uniqueEntries(entries)}
  function photoEntriesForJob(job){if(!job)return[];const entries=[...entriesFrom(job,'Fotografía inicial de la orden','Trabajo',job.data?.task||'')];linked('progress',job.id).sort((a,b)=>new Date(a.at)-new Date(b.at)).forEach((x,i)=>entries.push(...entriesFrom(x,x.data?.photoType||`Avance ${i+1}`,'Trabajo',x.data?.text||'')));linked('safety',job.id).sort((a,b)=>new Date(a.at)-new Date(b.at)).forEach((x,i)=>photoEntriesForSafety(x).forEach(p=>entries.push({...p,label:p.label||`OT · Desviación ${i+1}`})));linked('kpi',job.id).sort((a,b)=>new Date(a.at)-new Date(b.at)).forEach((x,i)=>entries.push(...entriesFrom(x,`Evaluación KPI ${i+1} · ${x.data?.technician||'Técnico'}`,'KPI',x.data?.notes||'')));return uniqueEntries(entries)}
  function photoEntriesForKpi(kpi){if(!kpi)return[];const entries=[...entriesFrom(kpi,`Evidencia KPI · ${kpi.data?.technician||'Técnico'}`,'KPI',kpi.data?.notes||'')],job=byType('job').find(x=>x.id===kpi.data?.jobId);if(job)photoEntriesForJob(job).forEach(p=>entries.push({...p,label:`OT asociada · ${p.label}`}));return uniqueEntries(entries)}
  function allPhotosForDay(day){const entries=[];byType('job').filter(x=>sameDay(x,day)).forEach(x=>entries.push(...photoEntriesForJob(x)));byType('safety').filter(x=>sameDay(x,day)&&!x.data?.jobId).forEach(x=>entries.push(...photoEntriesForSafety(x)));byType('walk').filter(x=>sameDay(x,day)).forEach(x=>entries.push(...photoEntriesForWalk(x)));byType('kpi').filter(x=>sameDay(x,day)).forEach(x=>entries.push(...entriesFrom(x,`KPI · ${x.data?.technician||'Técnico'}`,'KPI',x.data?.notes||'')));return uniqueEntries(entries)}
  function currentJob(){const stored=sessionStorage.getItem('mantpro-active-report-job');if(stored){const x=records().find(r=>r.id===stored&&r.type==='job');if(x)return x}const title=$('.report-sheet h1')?.textContent||'';return byType('job').find(j=>title.includes(j.data?.folio||'')&&title.includes(j.data?.equip||''))||null}

  async function loadImage(src){return new Promise((res,rej)=>{const i=new Image();if(!String(src).startsWith('data:'))i.crossOrigin='anonymous';i.onload=()=>res(i);i.onerror=()=>rej(new Error('No se pudo cargar la fotografía'));i.src=src})}
  async function toJpegData(src,max=1280,quality=.74){if(!src)throw new Error('Fotografía vacía');const i=await loadImage(src),iw=i.naturalWidth||i.width,ih=i.naturalHeight||i.height,s=Math.min(1,max/Math.max(iw,ih)),c=document.createElement('canvas');c.width=Math.max(1,Math.round(iw*s));c.height=Math.max(1,Math.round(ih*s));const x=c.getContext('2d',{alpha:false});x.fillStyle='#fff';x.fillRect(0,0,c.width,c.height);x.drawImage(i,0,0,c.width,c.height);return c.toDataURL('image/jpeg',quality)}
  async function optimizeInput(input,file){const src=await new Promise((res,rej)=>{const r=new FileReader();r.onload=()=>res(r.result);r.onerror=rej;r.readAsDataURL(file)}),data=await toJpegData(src,900,.60),blob=await(await fetch(data)).blob(),optimized=new File([blob],(file.name||'foto').replace(/\.[^.]+$/,'')+'.jpg',{type:'image/jpeg',lastModified:Date.now()}),dt=new DataTransfer();dt.items.add(optimized);input.files=dt.files}
  document.addEventListener('change',async e=>{const input=e.target;if(!(input instanceof HTMLInputElement)||input.id!=='photo'||input.dataset.optimized==='yes')return;const file=input.files?.[0];if(!file)return;e.preventDefault();e.stopImmediatePropagation();try{input.dataset.optimized='yes';await optimizeInput(input,file);input.dispatchEvent(new Event('change',{bubbles:true}))}catch(err){console.warn(err);input.dataset.optimized='yes';input.dispatchEvent(new Event('change',{bubbles:true}))}finally{setTimeout(()=>delete input.dataset.optimized,0)}},true);

  function appendPhotoAnnex(){const sheet=$('.report-sheet');if(!sheet||!$('[data-pdf-job]',sheet))return;const job=currentJob();if(!job)return;const photos=photoEntriesForJob(job),signature=photos.map(p=>`${p.src.length}:${p.src.slice(-18)}:${p.label}`).join('|')||'empty',existing=$('#mantpro-photo-annex');if(existing?.dataset.signature===signature)return;existing?.remove();const block=document.createElement('section');block.id='mantpro-photo-annex';block.className='report-photo-annex';block.dataset.signature=signature;block.innerHTML=`<h2>Registro fotográfico opcional (${photos.length})</h2>`+(photos.length?`<div class="report-photo-grid">${photos.map(p=>`<figure class="report-photo report-photo-fixed"><img src="${esc(p.src)}" alt="${esc(p.label)}" loading="eager"><figcaption><b>${esc(p.label)}</b><br><small>${fmt(p.capturedAt||p.record?.at)}${p.context?` · ${esc(p.context)}`:''}</small></figcaption></figure>`).join('')}</div>`:'<div class="empty">No se registraron fotografías. Esto no impide generar el informe.</div>');sheet.insertBefore(block,$('[data-pdf-job]',sheet))}
  function reportCenter(){const form=$('#report-form');if(!form||$('#mantpro-ehs-report-center'))return;const day=form.querySelector('[name="date"]')?.value||new Date().toISOString().slice(0,10),s=document.createElement('section');s.id='mantpro-ehs-report-center';s.className='section';s.innerHTML=`<h2>Informes KPI / EHS</h2><div class="callout"><b>Fotografías opcionales:</b> los PDF incorporan las imágenes disponibles con su designación. La ausencia de fotografías no bloquea ningún informe.</div><form id="ehs-report-controls"><label>Fecha de los informes<input id="ehs-report-date" type="date" value="${esc(day)}"></label><div class="grid"><button type="button" class="primary" data-pdf-ehs>Informe KPI / EHS</button><button type="button" data-pdf-safety-day>Seguridad y desvíos</button><button type="button" data-pdf-walks-day>Caminatas</button><button type="button" data-pdf-kpi-day>KPI técnicos</button></div></form><div id="ehs-individual-reports" class="section"></div>`;form.insertAdjacentElement('afterend',s);refreshIndividualReports()}
  function refreshIndividualReports(){const box=$('#ehs-individual-reports');if(!box)return;const day=selectedDay(),safety=byType('safety').filter(x=>sameDay(x,day)).sort((a,b)=>new Date(b.at)-new Date(a.at)),walks=byType('walk').filter(x=>sameDay(x,day)).sort((a,b)=>new Date(b.at)-new Date(a.at)),kpis=byType('kpi').filter(x=>sameDay(x,day)).sort((a,b)=>new Date(b.at)-new Date(a.at));box.innerHTML=`<h3>Informes individuales de la fecha</h3><div class="report-category"><b>Desviaciones / seguridad (${safety.length})</b><div class="list">${safety.length?safety.map(x=>`<button type="button" class="list-item" data-pdf-safety-id="${x.id}"><span>${esc(x.data?.category||'Desviación')} · ${esc(x.data?.area||'Área')}</span><small>${fmt(x.at)} · ${photoEntriesForSafety(x).length} foto(s)</small></button>`).join(''):'<div class="empty">Sin desvíos en la fecha.</div>'}</div></div><div class="report-category"><b>Caminatas (${walks.length})</b><div class="list">${walks.length?walks.map(x=>`<button type="button" class="list-item" data-pdf-walk-id="${x.id}"><span>${esc(x.data?.area||'Caminata')}</span><small>${fmt(x.data?.startAt||x.at)} · ${photoEntriesForWalk(x).length} foto(s)</small></button>`).join(''):'<div class="empty">Sin caminatas en la fecha.</div>'}</div></div><div class="report-category"><b>Evaluaciones KPI (${kpis.length})</b><div class="list">${kpis.length?kpis.map(x=>`<button type="button" class="list-item" data-pdf-kpi-id="${x.id}"><span>${esc(x.data?.technician||'Técnico')} · ${Number(x.data?.score||0)}%</span><small>${fmt(x.at)} · ${photoEntriesForKpi(x).length} evidencia(s)</small></button>`).join(''):'<div class="empty">Sin evaluaciones KPI en la fecha.</div>'}</div></div>`}
  let rt;const refreshUi=()=>{clearTimeout(rt);rt=setTimeout(()=>{appendPhotoAnnex();reportCenter();refreshIndividualReports()},110)},startObserver=()=>{new MutationObserver(refreshUi).observe($('#app')||document.body,{childList:true,subtree:true});refreshUi()};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',startObserver);else startObserver();
  document.addEventListener('click',e=>{const o=e.target.closest('[data-open-job],[data-report-job],[data-open-walk]');if(o?.dataset.openJob||o?.dataset.reportJob)sessionStorage.setItem('mantpro-active-report-job',o.dataset.openJob||o.dataset.reportJob);if(o?.dataset.openWalk)sessionStorage.setItem('mantpro-active-report-walk',o.dataset.openWalk)},true);
  document.addEventListener('submit',e=>{if(e.target?.id==='report-form'){const d=e.target.querySelector('[name="date"]')?.value;if(d)sessionStorage.setItem('mantpro-report-day',d)}},true);document.addEventListener('change',e=>{if(e.target?.id==='ehs-report-date'){sessionStorage.setItem('mantpro-report-day',e.target.value);refreshIndividualReports()}},true);

  const pdfName=v=>String(v||'MANTPRO').normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9_-]+/gi,'_').replace(/^_+|_+$/g,'')+'.pdf';
  function writer(title,subtitle){if(!window.jspdf?.jsPDF)throw new Error('El generador PDF no está disponible. Cierre y vuelva a abrir MANTPRO.');const doc=new window.jspdf.jsPDF({unit:'mm',format:'a4',compress:true}),margin=15,width=180;let y=18;const header=first=>{doc.setFillColor(18,27,35);doc.rect(0,0,210,first?38:18,'F');doc.setTextColor(255,139,24);doc.setFont('helvetica','bold');doc.setFontSize(first?18:11);doc.text('MANTPRO IA',margin,first?17:11);doc.setTextColor(255,255,255);if(first){doc.setFontSize(15);doc.text(title,margin,27,{maxWidth:width});doc.setFontSize(9);doc.text(subtitle||'',margin,34,{maxWidth:width})}y=first?46:25},ensure=h=>{if(y+h>278){doc.addPage();header(false)}},paragraph=(text,opt={})=>{const size=opt.size||10,lh=opt.lineHeight||4.6;doc.setFont('helvetica',opt.bold?'bold':'normal');doc.setFontSize(size);doc.setTextColor(...(opt.color||[33,43,54]));const lines=[];String(text??'No informado').replace(/\r/g,'').split('\n').forEach(p=>lines.push(...doc.splitTextToSize(p||' ',width)));ensure(lines.length*lh+2);doc.text(lines,margin,y);y+=lines.length*lh+(opt.after??2)},section=text=>{ensure(12);y+=3;doc.setFillColor(238,242,245);doc.rect(margin,y-5,width,9,'F');doc.setTextColor(18,61,92);doc.setFont('helvetica','bold');doc.setFontSize(12);doc.text(String(text),margin+3,y+1);y+=9},item=(head,body)=>{ensure(14);doc.setDrawColor(205,214,221);doc.setFillColor(249,250,251);doc.roundedRect(margin,y-4,width,7,1.5,1.5,'FD');doc.setTextColor(18,61,92);doc.setFont('helvetica','bold');doc.setFontSize(10);const l=doc.splitTextToSize(String(head),width-6);doc.text(l,margin+3,y);y+=l.length*4.2+3;paragraph(body,{size:9.5,after:4})},photo=async(entry,index)=>{try{const data=await toJpegData(entry.src,1500,.78),img=await loadImage(data),ratio=(img.naturalWidth||img.width)/(img.naturalHeight||img.height);let w=150,h=w/ratio;if(h>100){h=100;w=h*ratio}ensure(h+25);paragraph(`${index+1}. ${entry.label}`,{bold:true,size:9,after:2});doc.setDrawColor(160,170,180);doc.rect(margin,y,w,h);doc.addImage(data,'JPEG',margin,y,w,h,undefined,'FAST');y+=h+4;if(entry.context)paragraph(`Antecedente: ${entry.context}`,{size:8,color:[70,81,92],after:1});paragraph(`Registro: ${fmt(entry.capturedAt||entry.record?.at)}`,{size:8,color:[90,101,112],after:4});return true}catch(err){console.warn(err);paragraph(`${index+1}. ${entry.label}: no fue posible incrustar esta fotografía.`,{size:8,color:[150,55,55]});return false}},finish=name=>{const pages=doc.getNumberOfPages();for(let p=1;p<=pages;p++){doc.setPage(p);doc.setDrawColor(205,214,221);doc.line(margin,287,195,287);doc.setFont('helvetica','normal');doc.setFontSize(8);doc.setTextColor(90,101,112);doc.text(`Supervisor: ${supervisor()}`,margin,292);doc.text(`Página ${p} de ${pages}`,195,292,{align:'right'})}doc.save(pdfName(name))};header(true);return{paragraph,section,item,photo,finish}}
  const pauseMs=j=>(j.data?.pauses||[]).reduce((n,p)=>n+(p.end?new Date(p.end)-new Date(p.start):0),0),duration=(s,e)=>{if(!s||!e)return'No registrado';const ms=Math.max(0,new Date(e)-new Date(s)),h=Math.floor(ms/3600000),m=Math.floor(ms%3600000/60000);return`${h} h ${String(m).padStart(2,'0')} min`},netDuration=j=>{const d=j.data||{};if(!d.actualStart||!d.actualEnd)return'No registrado';const ms=Math.max(0,new Date(d.actualEnd)-new Date(d.actualStart)-pauseMs(j)),h=Math.floor(ms/3600000),m=Math.floor(ms%3600000/60000);return`${h} h ${String(m).padStart(2,'0')} min`},jobStatus=j=>{const d=j.data||{};if(d.actualEnd)return'Terminado';if(d.pauseStartAt)return'Pausado';if(d.actualStart)return'En ejecución';if(d.lotoStart)return'En bloqueo';if(d.docsStart)return'Documentación';return'Programado'};
  async function addPhotos(pdf,photos,empty='No se registraron fotografías. El informe se genera igualmente.'){if(!photos.length){pdf.paragraph(empty);return 0}let ok=0;for(let i=0;i<photos.length;i++)if(await pdf.photo(photos[i],i))ok++;return ok}

  async function downloadJobPdfFixed(){const j=currentJob();if(!j)throw new Error('No fue posible identificar la orden de trabajo.');const d=j.data||{},prog=linked('progress',j.id).sort((a,b)=>new Date(a.at)-new Date(b.at)),saf=linked('safety',j.id).sort((a,b)=>new Date(a.at)-new Date(b.at)),kpis=linked('kpi',j.id),photos=photoEntriesForJob(j),pdf=writer('Informe técnico de mantención',`${d.folio||'OT'} · ${d.equip||'Equipo no informado'}`);pdf.section('Identificación');pdf.paragraph(`Supervisor: ${supervisor()}\nPlanta / área: ${d.plant||'No informado'} / ${d.area||'No informado'}\nEquipo / TAG: ${d.equip||'No informado'}\nTrabajo: ${d.task||'No informado'}\nEstado: ${jobStatus(j)}\nFecha de emisión: ${fmt(new Date().toISOString())}`);pdf.section('Horario real y tiempos');pdf.item('Documentación de seguridad',`Inicio: ${fmt(d.docsStart)}\nTérmino: ${fmt(d.docsEnd)}\nDuración: ${duration(d.docsStart,d.docsEnd)}`);pdf.item('Bloqueo / aislamiento LOTO',`Inicio: ${fmt(d.lotoStart)}\nEnergía cero verificada: ${fmt(d.zeroVerifiedAt)}\nTérmino: ${fmt(d.lotoEnd)}\nDuración: ${duration(d.lotoStart,d.lotoEnd)}`);pdf.item('Ejecución real',`Inicio: ${fmt(d.actualStart)}\nTérmino: ${fmt(d.actualEnd)}\nDuración neta: ${netDuration(j)}\nAvance final: ${Number(d.progress||0)}%`);pdf.section('Actividades realizadas');if(!prog.length)pdf.paragraph('No informado.');prog.forEach((x,i)=>pdf.item(`Avance ${i+1} · ${x.data?.percent||0}% · ${fmt(x.at)}`,`${x.data?.text||'No informado'}${x.data?.findings?'\nHallazgo: '+x.data.findings:''}`));pdf.section('Desviaciones');if(!saf.length)pdf.paragraph('No se registraron desviaciones.');saf.forEach((x,i)=>pdf.item(`Desviación ${i+1} · ${x.data?.risk||'Riesgo no informado'}`,`${x.data?.category||'Sin categoría'}\nHallazgo: ${x.data?.text||'No informado'}\nAcción: ${x.data?.action||'No informada'}\nRecomendación: ${x.data?.recommendation||'No informada'}`));pdf.section('Evaluación de técnicos');if(!kpis.length)pdf.paragraph('No informado.');kpis.forEach(x=>pdf.item(`${x.data?.technician||'Técnico'} · ${x.data?.score||0}%`,x.data?.notes||'Sin observaciones.'));pdf.section(`Registro fotográfico opcional (${photos.length})`);const ok=await addPhotos(pdf,photos);pdf.section('Trazabilidad');pdf.paragraph(`Fotografías disponibles: ${photos.length}. Fotografías incorporadas: ${ok}. La ausencia de imágenes no afecta la validez del informe.`);pdf.finish(`Informe_${d.folio||j.id}`);showToast(`Informe descargado con ${ok} fotografía(s).`)}
  async function downloadDailyPdfFixed(){const day=selectedDay(),jobs=byType('job').filter(x=>sameDay(x,day)),safety=byType('safety').filter(x=>sameDay(x,day)),walks=byType('walk').filter(x=>sameDay(x,day)),kpis=byType('kpi').filter(x=>sameDay(x,day)),photos=allPhotosForDay(day),pdf=writer('Informe general diario',new Date(day+'T12:00:00').toLocaleDateString('es-CL',{dateStyle:'full'}));pdf.section('Resumen ejecutivo');pdf.paragraph(`Supervisor: ${supervisor()}\nTrabajos: ${jobs.length}\nTrabajos terminados: ${jobs.filter(x=>x.data?.actualEnd).length}\nDesviaciones: ${safety.length}\nCaminatas: ${walks.length}\nEvaluaciones KPI: ${kpis.length}\nFotografías disponibles: ${photos.length}`);pdf.section('Trabajos');jobs.forEach(j=>pdf.item(`${j.data?.folio||'OT'} · ${j.data?.equip||'Equipo'}`,`Trabajo: ${j.data?.task||'No informado'}\nInicio: ${fmt(j.data?.actualStart)}\nTérmino: ${fmt(j.data?.actualEnd)}\nAvance: ${Number(j.data?.progress||0)}%`));if(!jobs.length)pdf.paragraph('Sin trabajos registrados.');pdf.section('Seguridad y desvíos');safety.forEach((x,i)=>pdf.item(`${i+1}. ${x.data?.category||'Desviación'} · ${x.data?.risk||'Riesgo no informado'}`,`${x.data?.area||'Área no informada'}\n${x.data?.text||'Sin descripción'}\nAcción: ${x.data?.action||'No informada'}`));if(!safety.length)pdf.paragraph('Sin desviaciones registradas.');pdf.section('Caminatas');walks.forEach((x,i)=>pdf.item(`${i+1}. ${x.data?.area||'Área'}`,`Inicio: ${fmt(x.data?.startAt||x.at)}\nTérmino: ${fmt(x.data?.endAt)}\nObjetivo: ${x.data?.objective||'No informado'}`));if(!walks.length)pdf.paragraph('Sin caminatas registradas.');pdf.section(`Registro fotográfico opcional (${photos.length})`);const ok=await addPhotos(pdf,photos);pdf.finish(`Informe_Diario_${day}`);showToast(`Informe diario descargado con ${ok} fotografía(s).`)}
  async function downloadSafetyPdf(r){if(!r)throw new Error('Desviación no disponible.');const d=r.data||{},photos=photoEntriesForSafety(r),pdf=writer('Informe de desviación de seguridad',`${d.category||'Desviación'} · ${d.area||'Área no informada'}`);pdf.section('Identificación');pdf.paragraph(`Supervisor: ${supervisor()}\nFecha y hora: ${fmt(r.at)}\nOrigen: ${d.origin||'No informado'}\nPlanta: ${d.plant||'No informada'}\nÁrea: ${d.area||'No informada'}\nCategoría: ${d.category||'No informada'}\nNivel de riesgo: ${d.risk||'No informado'}`);pdf.section('Hallazgo y control');pdf.item('Descripción objetiva',d.text||'No informada');pdf.item('Acción inmediata',d.action||'No informada');pdf.item('Medida correctiva / recomendación',d.recommendation||'No informada');pdf.item('Seguimiento',`Responsable: ${d.responsible||'No informado'}\nFecha compromiso: ${d.dueDate||'No informada'}\nEstado: ${d.closedAt?'Cerrada':'Abierta'}\nCierre verificado: ${fmt(d.closedAt)}`);pdf.section(`Evidencia fotográfica opcional (${photos.length})`);const ok=await addPhotos(pdf,photos);pdf.finish(`Desviacion_${d.category||r.id}_${dateKey(r.at)}`);showToast(`Informe de desviación con ${ok} fotografía(s).`)}
  async function downloadWalkPdf(w){if(!w)throw new Error('Caminata no disponible.');const d=w.data||{},findings=byType('safety').filter(x=>x.data?.walkId===w.id).sort((a,b)=>new Date(a.at)-new Date(b.at)),photos=photoEntriesForWalk(w),pdf=writer('Informe de caminata de seguridad',`${d.plant||'Planta'} · ${d.area||'Área no informada'}`);pdf.section('Identificación');pdf.paragraph(`Supervisor: ${supervisor()}\nInicio: ${fmt(d.startAt||w.at)}\nTérmino: ${fmt(d.endAt)}\nPlanta: ${d.plant||'No informada'}\nÁreas recorridas: ${d.area||'No informadas'}\nObjetivo: ${d.objective||'No informado'}\nParticipantes: ${d.participants||'No informados'}`);pdf.section(`Hallazgos (${findings.length})`);if(!findings.length)pdf.paragraph('No se registraron hallazgos.');findings.forEach((x,i)=>pdf.item(`Hallazgo ${i+1} · ${x.data?.category||'Sin categoría'} · Riesgo ${x.data?.risk||'No informado'}`,`Ubicación: ${x.data?.area||'No informada'}\nDescripción: ${x.data?.text||'No informada'}\nAcción: ${x.data?.action||'No informada'}\nRecomendación: ${x.data?.recommendation||'No informada'}`));pdf.section(`Registro fotográfico opcional (${photos.length})`);const ok=await addPhotos(pdf,photos);pdf.finish(`Caminata_${dateKey(d.startAt||w.at)}_${d.area||w.id}`);showToast(`Informe de caminata con ${ok} fotografía(s).`)}
  async function downloadKpiPdf(k){if(!k)throw new Error('Evaluación KPI no disponible.');const d=k.data||{},job=byType('job').find(x=>x.id===d.jobId),photos=photoEntriesForKpi(k),pdf=writer('Informe KPI de desempeño técnico',`${d.technician||'Técnico'} · ${Number(d.score||0)}%`);pdf.section('Identificación');pdf.paragraph(`Supervisor: ${supervisor()}\nTécnico: ${d.technician||'No informado'}\nFecha: ${fmt(k.at)}\nOrden asociada: ${job?.data?.folio||'No informada'}\nEquipo: ${job?.data?.equip||'No informado'}\nPuntaje final: ${Number(d.score||0)}%`);pdf.section('Criterios');pdf.item('Seguridad y controles críticos',`${d.safety||'No informado'} / 5`);pdf.item('Calidad técnica',`${d.quality||'No informado'} / 5`);pdf.item('Cumplimiento',`${d.compliance||'No informado'} / 5`);pdf.item('Productividad responsable',`${d.productivity||'No informado'} / 5`);pdf.item('Documentación',`${d.documentation||'No informado'} / 5`);pdf.item('Coordinación',`${d.communication||'No informado'} / 5`);pdf.section('Observaciones');pdf.paragraph(`Correcto a la primera: ${d.firstTimeRight?'Sí':'No'}\nRetrabajo: ${d.rework?'Sí':'No'}\nFactores externos: ${d.externalFactors?'Sí':'No'}\nObservaciones: ${d.notes||'No informadas'}\nCapacitación: ${d.training||'No informada'}`);pdf.section(`Evidencias fotográficas opcionales (${photos.length})`);const ok=await addPhotos(pdf,photos);pdf.finish(`KPI_${d.technician||k.id}_${dateKey(k.at)}`);showToast(`Informe KPI con ${ok} fotografía(s).`)}
  async function downloadSafetyDayPdf(day){const safety=byType('safety').filter(x=>sameDay(x,day)).sort((a,b)=>new Date(a.at)-new Date(b.at)),photos=uniqueEntries(safety.flatMap(photoEntriesForSafety)),pdf=writer('Informe diario de seguridad y desvíos',new Date(day+'T12:00:00').toLocaleDateString('es-CL',{dateStyle:'full'}));const high=safety.filter(x=>/alto|crítico/i.test(x.data?.risk||'')).length,closed=safety.filter(x=>x.data?.closedAt).length;pdf.section('Indicadores EHS');pdf.paragraph(`Supervisor: ${supervisor()}\nDesviaciones / observaciones: ${safety.length}\nRiesgos altos o críticos: ${high}\nCerradas: ${closed}\nAbiertas: ${safety.length-closed}\nFotografías disponibles: ${photos.length}`);pdf.section('Detalle');if(!safety.length)pdf.paragraph('Sin registros.');safety.forEach((x,i)=>pdf.item(`${i+1}. ${x.data?.category||'Desviación'} · ${x.data?.risk||'Riesgo no informado'}`,`Hora: ${fmt(x.at)}\nÁrea: ${x.data?.area||'No informada'}\nHallazgo: ${x.data?.text||'No informado'}\nAcción: ${x.data?.action||'No informada'}\nRecomendación: ${x.data?.recommendation||'No informada'}`));pdf.section(`Evidencia fotográfica opcional (${photos.length})`);const ok=await addPhotos(pdf,photos);pdf.finish(`Seguridad_Desvios_${day}`);showToast(`Informe de seguridad con ${ok} fotografía(s).`)}
  async function downloadWalksDayPdf(day){const walks=byType('walk').filter(x=>sameDay(x,day)).sort((a,b)=>new Date(a.at)-new Date(b.at)),photos=uniqueEntries(walks.flatMap(photoEntriesForWalk)),pdf=writer('Informe diario de caminatas',new Date(day+'T12:00:00').toLocaleDateString('es-CL',{dateStyle:'full'}));pdf.section('Resumen');pdf.paragraph(`Supervisor: ${supervisor()}\nCaminatas realizadas: ${walks.length}\nFotografías disponibles: ${photos.length}`);if(!walks.length)pdf.paragraph('Sin caminatas registradas.');walks.forEach((w,i)=>{const f=byType('safety').filter(x=>x.data?.walkId===w.id);pdf.section(`Caminata ${i+1} · ${w.data?.area||'Área'}`);pdf.paragraph(`Inicio: ${fmt(w.data?.startAt||w.at)}\nTérmino: ${fmt(w.data?.endAt)}\nObjetivo: ${w.data?.objective||'No informado'}\nParticipantes: ${w.data?.participants||'No informados'}\nHallazgos: ${f.length}`)});pdf.section(`Registro fotográfico opcional (${photos.length})`);const ok=await addPhotos(pdf,photos);pdf.finish(`Caminatas_${day}`);showToast(`Informe de caminatas con ${ok} fotografía(s).`)}
  async function downloadKpiDayPdf(day){const kpis=byType('kpi').filter(x=>sameDay(x,day)).sort((a,b)=>new Date(a.at)-new Date(b.at)),photos=uniqueEntries(kpis.flatMap(photoEntriesForKpi)),pdf=writer('Informe diario KPI de técnicos',new Date(day+'T12:00:00').toLocaleDateString('es-CL',{dateStyle:'full'})),avg=kpis.length?Math.round(kpis.reduce((n,x)=>n+Number(x.data?.score||0),0)/kpis.length):0;pdf.section('Resumen KPI');pdf.paragraph(`Supervisor: ${supervisor()}\nEvaluaciones: ${kpis.length}\nPromedio: ${kpis.length?avg+'%':'No informado'}\nRetrabajos: ${kpis.filter(x=>x.data?.rework).length}\nEvidencias disponibles: ${photos.length}`);pdf.section('Detalle');if(!kpis.length)pdf.paragraph('Sin evaluaciones.');kpis.forEach((x,i)=>pdf.item(`${i+1}. ${x.data?.technician||'Técnico'} · ${Number(x.data?.score||0)}%`,x.data?.notes||'Sin observaciones.'));pdf.section(`Evidencias fotográficas opcionales (${photos.length})`);const ok=await addPhotos(pdf,photos);pdf.finish(`KPI_Tecnicos_${day}`);showToast(`Informe KPI con ${ok} fotografía(s).`)}
  async function downloadEhsPdf(day){const safety=byType('safety').filter(x=>sameDay(x,day)),walks=byType('walk').filter(x=>sameDay(x,day)),kpis=byType('kpi').filter(x=>sameDay(x,day)),jobs=byType('job').filter(x=>sameDay(x,day)),photos=allPhotosForDay(day),pdf=writer('Informe KPI / EHS',new Date(day+'T12:00:00').toLocaleDateString('es-CL',{dateStyle:'full'})),high=safety.filter(x=>/alto|crítico/i.test(x.data?.risk||'')).length,closed=safety.filter(x=>x.data?.closedAt).length,avg=kpis.length?Math.round(kpis.reduce((n,x)=>n+Number(x.data?.score||0),0)/kpis.length):0;pdf.section('Panel KPI / EHS');pdf.paragraph(`Supervisor: ${supervisor()}\nTrabajos: ${jobs.length}\nCaminatas: ${walks.length}\nDesviaciones / observaciones: ${safety.length}\nRiesgos altos o críticos: ${high}\nCierre de desvíos: ${safety.length?Math.round(closed/safety.length*100)+'%':'No informado'}\nEvaluaciones técnicas: ${kpis.length}\nPromedio KPI: ${kpis.length?avg+'%':'No informado'}\nFotografías disponibles: ${photos.length}`);pdf.section('Seguridad y desvíos');if(!safety.length)pdf.paragraph('Sin registros.');safety.forEach((x,i)=>pdf.item(`${i+1}. ${x.data?.category||'Desviación'} · ${x.data?.risk||'Riesgo no informado'}`,`${x.data?.area||'Área no informada'}\n${x.data?.text||'Sin descripción'}\nAcción: ${x.data?.action||'No informada'}`));pdf.section('Caminatas');if(!walks.length)pdf.paragraph('Sin caminatas.');walks.forEach((x,i)=>pdf.item(`${i+1}. ${x.data?.area||'Área'}`,`Inicio: ${fmt(x.data?.startAt||x.at)}\nTérmino: ${fmt(x.data?.endAt)}\nObjetivo: ${x.data?.objective||'No informado'}`));pdf.section('KPI de técnicos');if(!kpis.length)pdf.paragraph('Sin evaluaciones.');kpis.forEach((x,i)=>pdf.item(`${i+1}. ${x.data?.technician||'Técnico'} · ${Number(x.data?.score||0)}%`,x.data?.notes||'Sin observaciones.'));pdf.section(`Registro fotográfico integral opcional (${photos.length})`);const ok=await addPhotos(pdf,photos);pdf.section('Control de integridad');pdf.paragraph(`Fotografías disponibles: ${photos.length}. Fotografías incorporadas: ${ok}. Cada imagen conserva su designación. Los informes pueden emitirse sin fotografías.`);pdf.finish(`KPI_EHS_${day}`);showToast(`Informe KPI / EHS con ${ok} fotografía(s).`)}

  function showToast(text){const t=$('#toast');if(!t){alert(text);return}t.textContent=text;t.hidden=false;setTimeout(()=>t.hidden=true,4200)}
  async function runButton(b,text,task){b.disabled=true;const old=b.textContent;b.textContent=text;try{await task()}catch(e){console.error(e);alert(e.message||'No fue posible generar el informe.')}finally{b.disabled=false;b.textContent=old}}
  document.addEventListener('click',e=>{const b=e.target.closest('[data-pdf-job],[data-pdf-daily],[data-pdf-ehs],[data-pdf-safety-day],[data-pdf-walks-day],[data-pdf-kpi-day],[data-pdf-safety-id],[data-pdf-walk-id],[data-pdf-kpi-id]');if(!b)return;e.preventDefault();e.stopImmediatePropagation();const day=selectedDay();if(b.matches('[data-pdf-job]'))return void runButton(b,'Preparando fotografías…',downloadJobPdfFixed);if(b.matches('[data-pdf-daily]'))return void runButton(b,'Preparando fotografías…',downloadDailyPdfFixed);if(b.matches('[data-pdf-ehs]'))return void runButton(b,'Generando KPI / EHS…',()=>downloadEhsPdf(day));if(b.matches('[data-pdf-safety-day]'))return void runButton(b,'Generando seguridad…',()=>downloadSafetyDayPdf(day));if(b.matches('[data-pdf-walks-day]'))return void runButton(b,'Generando caminatas…',()=>downloadWalksDayPdf(day));if(b.matches('[data-pdf-kpi-day]'))return void runButton(b,'Generando KPI…',()=>downloadKpiDayPdf(day));if(b.dataset.pdfSafetyId)return void runButton(b,'Generando desvío…',()=>downloadSafetyPdf(records().find(x=>x.id===b.dataset.pdfSafetyId)));if(b.dataset.pdfWalkId)return void runButton(b,'Generando caminata…',()=>downloadWalkPdf(records().find(x=>x.id===b.dataset.pdfWalkId)));if(b.dataset.pdfKpiId)return void runButton(b,'Generando KPI…',()=>downloadKpiPdf(records().find(x=>x.id===b.dataset.pdfKpiId)))},true);
  window.MANTPRO_PHOTO_REPORT_FIX={build:BUILD,photoCandidates,photoEntriesForJob,photoEntriesForSafety,photoEntriesForWalk,photoEntriesForKpi,allPhotosForDay,downloadJobPdfFixed,downloadDailyPdfFixed,downloadEhsPdf,downloadSafetyDayPdf,downloadWalksDayPdf,downloadKpiDayPdf,downloadSafetyPdf,downloadWalkPdf,downloadKpiPdf};
})();